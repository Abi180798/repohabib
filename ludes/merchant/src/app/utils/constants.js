import { Base64 } from "js-base64";

export const TOKEN = Base64.encode("patra-token");
export const USER = Base64.encode("patra-user");
export const CHECKED = Base64.encode("patra-checked");
export const REGION = Base64.encode("patra-region");
export const WA = Base64.encode("patra-wa");
export const PASS = Base64.encode("patra-pass");
